using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace NormalUtils { 
    public class Utils
    {
    public int Add(int first, int second)
    {
        return first + second;
    }

    public Vector2 Add(Vector2 first, Vector2 second)
    {
        return first + second;
    }
        //1 Static method
        //static public float Add(float first, float second)
        //{
        //    return first + second;
        //}
    }
}
